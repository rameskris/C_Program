#include <stdio.h>
#include <stdint.h>

int main()
{

    uint8_t a = 12;
    uint8_t b = 5;
    printf("A & B %u\n", a & b);
    printf("A | B %u\n", a | b);

    printf("A ^ B %u\n", a & b);
    printf("A << B %u\n", a << b);
    printf("A >> B %f
    
    \n", a >> b);

    return 0;
}